const app=document.getElementById('app');app.innerHTML=`<section class="panel"><h2>Add Your Location</h2><p>Only country and an optional broad region are displayed. Never enter a street address.</p><form id="f"><div class="grid"><div><label>Chess.com Username</label><input name="username" required maxlength="50"></div><div><label>Country</label><input name="country" required maxlength="80"></div></div><label>State / Province / Region (optional)</label><input name="region" maxlength="100"><div class="button-row"><button class="btn">Join the Map</button></div></form><div data-status hidden></div></section><section class="panel"><h2>ACFA Around the World</h2><div id="summary" class="grid"><div class="empty">No locations loaded yet.</div></div></section>`;
document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();if(!acfaRequireDb())return;const d=Object.fromEntries(new FormData(e.target));const {error}=await acfaDb.from('acfa_member_locations').upsert({...d,status:'approved'},{onConflict:'username'});if(error)return acfaSetStatus(error.message,'error');e.target.reset();acfaSetStatus('Location added to the community map.','success');});
(async()=>{if(!acfaDb)return;const {data}=await acfaDb.from('acfa_member_locations').select('*').eq('status','approved');if(!data?.length)return;const grouped={};data.forEach(x=>{grouped[x.country]=(grouped[x.country]||0)+1});document.getElementById('summary').innerHTML=Object.entries(grouped).sort((a,b)=>b[1]-a[1]).map(([country,count])=>`<article class="card"><strong>${acfaEscape(country)}</strong><div class="meta">${count} member${count===1?'':'s'}</div><div class="bar"><span style="width:${Math.min(100,count/Math.max(...Object.values(grouped))*100)}%"></span></div></article>`).join('');})();
