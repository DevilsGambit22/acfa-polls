const app=document.getElementById('app');app.innerHTML=`<section class="panel"><h2>Submit a Club Suggestion</h2><form id="f"><div class="grid"><div><label>Chess.com Username</label><input name="username" required maxlength="50"></div><div><label>Category</label><select name="category"><option>Events</option><option>Matches</option><option>Forums</option><option>Streaming</option><option>Community</option><option>Website</option><option>Club Rules</option><option>Other</option></select></div></div><label>Suggestion Title</label><input name="title" required maxlength="120"><label>Description</label><textarea name="description" required maxlength="1200"></textarea><div class="button-row"><button class="btn">Submit Suggestion</button></div></form><div data-status hidden></div></section><section class="panel"><h2>Community Ideas</h2><div id="list" class="list"><div class="empty">No approved suggestions loaded yet.</div></div></section>`;
document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();if(!acfaRequireDb())return;const d=Object.fromEntries(new FormData(e.target));const {error}=await acfaDb.from('acfa_suggestions').insert({...d,status:'pending'});if(error)return acfaSetStatus(error.message,'error');e.target.reset();acfaSetStatus('Suggestion submitted for moderation.','success');});
(async()=>{if(!acfaDb)return;const {data}=await acfaDb.from('acfa_suggestions').select('*').in('status',['approved','under_consideration','planned','completed']).order('created_at',{ascending:false});if(!data?.length)return;document.getElementById('list').innerHTML=data.map(x=>`<article class="list-item"><strong>${acfaEscape(x.title)}</strong> <span class="badge">${acfaEscape(x.status.replaceAll('_',' '))}</span><div class="meta">${acfaEscape(x.category)} • ${acfaEscape(x.username)}</div><p>${acfaEscape(x.description)}</p></article>`).join('');})();
