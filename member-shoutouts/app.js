const app=document.getElementById('app');app.innerHTML=`<section class="panel"><h2>Send a Shoutout</h2><form id="f"><div class="grid"><div><label>From Username</label><input name="from_username" required maxlength="50"></div><div><label>To Username</label><input name="to_username" required maxlength="50"></div></div><label>Message</label><textarea name="message" required maxlength="500"></textarea><div class="button-row"><button class="btn">Submit for Review</button></div></form><div data-status hidden></div></section><section class="panel"><h2>Community Shoutouts</h2><div id="list" class="list"><div class="empty">No approved shoutouts loaded yet.</div></div></section>`;
document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();if(!acfaRequireDb())return;const d=Object.fromEntries(new FormData(e.target));const {error}=await acfaDb.from('acfa_shoutouts').insert({...d,status:'pending'});if(error)return acfaSetStatus(error.message,'error');e.target.reset();acfaSetStatus('Shoutout submitted for moderation.','success');});
(async()=>{if(!acfaDb)return;const {data}=await acfaDb.from('acfa_shoutouts').select('*').eq('status','approved').order('created_at',{ascending:false}).limit(100);if(!data?.length)return;document.getElementById('list').innerHTML=data.map(x=>`<article class="list-item"><strong>👏 ${acfaEscape(x.to_username)}</strong><p>${acfaEscape(x.message)}</p><div class="meta">— ${acfaEscape(x.from_username)}</div></article>`).join('');})();
