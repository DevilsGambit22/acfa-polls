const app=document.getElementById('app');
app.innerHTML=`<section class="panel"><h2>Submit an Opening</h2><p>Approved entries become part of ACFA's community opening encyclopedia.</p><form id="openingForm"><div class="grid"><div><label>Chess.com Username</label><input name="username" required maxlength="50"></div><div><label>Opening Name</label><input name="opening_name" required maxlength="100"></div><div><label>Color</label><select name="color"><option>White</option><option>Black</option><option>Either</option></select></div><div><label>Difficulty</label><select name="difficulty"><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></div></div><label>Why do you recommend it?</label><textarea name="recommendation" required maxlength="800"></textarea><label>Optional Study Link</label><input name="resource_url" type="url"><div class="button-row"><button class="btn">Submit for Review</button></div></form><div data-status hidden></div></section><section class="panel"><h2>Approved Openings</h2><div id="openingList" class="list"><div class="empty">No approved openings loaded yet.</div></div></section>`;
const form=document.getElementById('openingForm');
form.addEventListener('submit',async e=>{e.preventDefault();if(!acfaRequireDb())return;const d=Object.fromEntries(new FormData(form));const {error}=await acfaDb.from('acfa_openings').insert({...d,status:'pending'});if(error)return acfaSetStatus(error.message,'error');form.reset();acfaSetStatus('Opening submitted for moderation.','success');});
(async()=>{if(!acfaDb)return;const {data}=await acfaDb.from('acfa_openings').select('*').eq('status','approved').order('created_at',{ascending:false});if(!data?.length)return;document.getElementById('openingList').innerHTML=data.map(x=>`<article class="list-item"><strong>${acfaEscape(x.opening_name)}</strong> <span class="badge">${acfaEscape(x.color)}</span><div class="meta">${acfaEscape(x.difficulty)} • Recommended by ${acfaEscape(x.username)}</div><p>${acfaEscape(x.recommendation)}</p>${x.resource_url?`<a class="btn secondary" target="_blank" rel="noopener" href="${acfaEscape(x.resource_url)}">Open Resource</a>`:''}</article>`).join('');})();
